const employeeName = "Maya";
const department = "Engineering";
const introduction = `${employeeName} works in ${department}.`;

console.log(introduction);
console.log(`A calculation can be included too: 2 + 3 = ${2 + 3}`);